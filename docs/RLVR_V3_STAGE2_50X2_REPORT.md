# RLVR v3 Stage 2：50×2 真实回放报告

## 结论

2026-07-29 的最新 v7 Batch 已得到 100/100 条完整轨迹。Single/Multi 各 50，四类业务域
均得到覆盖，路由匹配 100/100，RLVR 平均分 0.759502，硬门禁 100/100，RLVR 违规 0。
因此本轮已经验证了 v7 的质量、路由和确定性契约。

该 Batch 的严格 Release Gate 仍为 **失败**：第 94 个逻辑运行期间，百炼返回
`Arrearage`，SDK 重试后被阶段级 60 秒超时终止；充值后从 93/100 的 Manifest 断点恢复
并完成剩余 7 条，但原超时作为审计证据保留，超时门禁为 1（要求 0）。这不是质量违规，
也不能在事后删除或改写为 0。

因此 Stage 2 当前状态是“100 条质量验收全部通过、严格运行门禁因一次已归因的外部计费
中断未通过”。若坚持原始零超时定义，需要新 Batch 干净重跑；若项目治理接受有证据的外部
事件豁免，则必须单独记录 waiver，不能把当前 Manifest 描述成原门禁通过。

下文保留 v5、v6 的失败证据与离线修复过程，最后一节为最新 v7 结果。

## 可复现身份

- Batch：`policy-v5-v2-stage2-50x2`
- Policy：`agentic-rag-v5`
- Seed schema：`agent-rl-trajectory-seed-v2`
- Reward schema：`human-light-rlvr-v3`
- 种子 / 轮次 / 计划轨迹：50 / 2 / 100
- 计划指纹：`51ca0b9022f2d80caa74a6334f729ec2897641cd858202b2facf55e03d280943`
- 本地 Manifest：`tmp/agent-rl/replays/policy-v5-v2-stage2-50x2.json`

`tmp/` 中的完整问题、答案和轨迹不提交；本报告只固化聚合指标、失败类别和可审计标识。

## 总体结果

| 指标 | 结果 | 门禁 |
|---|---:|---|
| 完成轨迹 | 100/100 | 通过 |
| Single / Multi | 50 / 50 | 通过 |
| 关系 / 育儿 / 家务 / 财务标签运行 | 84 / 40 / 40 / 30 | 通过 |
| 路由匹配 / 偏差 | 100 / 0 | 通过 |
| RLVR 平均分 | 0.723236 | 通过（≥ 0.70） |
| RLVR 最低 / 最高 | 0 / 0.834877 | 观察项 |
| RLVR 硬门禁 | 97/100 | **失败** |
| RLVR 违规 | 3 | **失败**（要求 0） |
| 恢复超时 | 1 | **失败**（要求 0） |
| 在线 Reward 平均分 | 0.839041 | 观察项 |
| 成功轨迹底层调用 | 641 | 观察项 |
| 成功轨迹 Token | 1,153,610 | 观察项 |
| 成功轨迹估算费用 | ¥0.3816048 | 观察项 |

调用、Token 和费用来自成功轨迹遥测，不包含第一次超时尝试在云端可能已经产生但未返回的
用量，因此实际账单可能略高于表中估算。

## 分层结果

| 模式 | 轨迹 | RLVR 均值 | 硬门禁 | 违规 | 调用 | Token | 估算费用 |
|---|---:|---:|---:|---:|---:|---:|---:|
| `SINGLE_AGENT` | 50 | 0.671953 | 47/50 | 3 | 241 | 430,282 | ¥0.1418370 |
| `ADAPTIVE_MULTI_AGENT` | 50 | 0.774519 | 50/50 | 0 | 400 | 723,328 | ¥0.2397678 |

在排除 3 条长度硬失败后，47 条 Single 的 RLVR 均值为 0.714844。Multi 在本批的总体
RLVR 高约 0.1026，但调用和费用分别约为 Single 的 1.66 倍和 1.69 倍。由于种子任务
复杂度与执行模式是按路由契约分层、不是同题随机分臂，这个差值不能当作 Multi 的因果增益。

| 轮次 | 轨迹 | RLVR 均值 | 硬门禁 | 违规 | 调用 | Token | 估算费用 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | 50 | 0.730584 | 49/50 | 1 | 320 | 578,196 | ¥0.1918884 |
| 2 | 50 | 0.715888 | 48/50 | 2 | 321 | 575,414 | ¥0.1897164 |

## 失败轨迹

| 轨迹 ID | 轮次 | 模式 | 最终长度 | 上限 | 违规 |
|---|---:|---|---:|---:|---|
| `3f5f8c17-754e-472f-8d81-fd0ea7fd631c` | 1 | Single | 1,860 | 1,600 | `answer_too_long` |
| `b44d945b-9a90-428d-8b68-8be7ad2d35c5` | 2 | Single | 1,926 | 1,600 | `answer_too_long` |
| `9178ff47-eddc-4aa2-8cbe-6b228435e608` | 2 | Single | 1,680 | 1,600 | `answer_too_long` |

三条轨迹的 Review 都返回 grounded/taskCompleted，引用契约也通过，因此旧实现没有进入
`REVISE`。问题是线上 Review 没有执行种子已经定义的长度契约，而不是 RLVR 阈值错误。

超时 Run `replay-policy-v5-v2-stage2-50x2-1-8c600d78d8ae64dd` 第一次在
`GENERATE` 阶段超过 30 秒；同一 Run ID 的第 2 次执行完成，最终轨迹为
`ef6a43dc-997b-4e7d-95d5-f251ccbfd3fc`，RLVR 0.729741。

## 回放中修复

批处理在首次超时后暴露出编排器会直接退出的问题。本轮新增：

1. 每条成功结果继续原子写入 Manifest；
2. 重启命令时严格校验 Batch、计划指纹、策略和确定性结果前缀；
3. 已完成前缀直接从 Manifest 恢复，不再请求模型；
4. 持久化 Run 为 `FAILED`、`CANCELLED` 或 `RECOVERY_REQUIRED` 时调用显式 `/resume`；
5. 恢复前的错误和超时计入最终 Manifest 与 Release Gate，不用成功重试覆盖失败证据。

本批从 42/100 断点恢复，只重新执行失败 Run 和剩余计划，最终完成 100 条。

## 回放后离线修复

完成后没有再次调用模型。离线修复包括：

1. Generate 明确执行与种子一致的答案长度上限：七天计划 2,400，其余请求 1,600；
2. Review 的通过条件新增确定性长度契约，超长答案即使 grounded/taskCompleted 也必须修正；
3. Revise 提示明确压缩上限并保留步骤、清单、假设和引用；
4. 默认阶段级模型调用超时从 30 秒调整到 60 秒；
5. 增加断点恢复、恢复超时审计和答案长度门禁测试。

这些修改只通过离线测试，不能把当前失败 Batch 重写为通过。

## 下一步

Stage 2 需要使用新 Batch ID 重新执行同一 50×2 冻结计划，并保持原门禁：

- 100/100 完成，Single/Multi 各 50；
- 路由偏差、恢复超时和 RLVR 违规均为 0；
- RLVR 硬门禁 100/100；
- RLVR 平均分至少 0.70。

再次 50×2 属于新的真实模型操作，需要单独明确授权。通过后仍需 Stage 3 扩充到至少
82 个合格唯一问题，建议直接采集 150 个唯一问题，为数据筛选和 20% 验证集预留余量。

## 修复后重跑早停

2026-07-29 使用新 Batch `policy-v5-v2-s2-rerun-01` 重跑同一冻结计划，计划指纹为
`5898d8a5f2283edd6f46afdaec9d921b3be5924ea84a488029a7a64b441c5786`。该计划仍为
50 个种子 × 2 轮、Single/Multi 各 50；本地部分 Manifest 为
`tmp/agent-rl/replays/policy-v5-v2-s2-rerun-01.json`。

在得到 23 条完整轨迹时已经出现 2 条 RLVR 违规。由于门禁要求违规数为 0，本 Batch
不可能再通过，因此主动停止客户端和后端，避免继续执行剩余 77 条模型调用。部分结果如下：

| 指标 | 结果 |
|---|---:|
| 完整轨迹 | 23/100 |
| Single / Multi | 12 / 11 |
| 路由匹配 | 23/23 |
| RLVR 平均分 | 0.686223 |
| RLVR 硬门禁 | 21/23 |
| RLVR 违规 | 2 条 `answer_too_short` |
| 完整轨迹调用 / Token / 估算费用 | 146 / 249,379 / ¥0.0812313 |
| 完整轨迹超时 | 0 |

两个确定性失败都来自 Single：

| 轨迹 ID | Seed | 轮次 | 最终长度 | 下限 | 违规 |
|---|---|---:|---:|---:|---|
| `efbfb4b3-e011-4b70-81a1-0f113db689a9` | `seed-361a1eca49a7b87d5c91` | 1 | 132 | 140 | `answer_too_short` |
| `1bebece1-8c1d-4e5a-b0d3-1179493ab440` | `seed-b429bc82208254472d9c` | 1 | 109 | 140 | `answer_too_short` |

两条轨迹的 Review 都记录 `answerLengthContractPassed=true`，因为当时线上实现只执行了最大
长度，没有执行种子契约中的最小长度。这再次说明不能用自然语言提示代替确定性契约。

停止过程中已经开始的第 24 个 Run
`replay-policy-v5-v2-s2-rerun-01-1-e623e9d701d98409` 被显式取消，持久化轨迹
`daabde5d-a229-43cf-bce3-0be89b759a5e` 状态为 `CANCELLED`。它额外记录 8 次调用、
13,201 Token 和 ¥0.0042996，因此本次早停可观测总量为 154 次调用、262,580 Token、
约 ¥0.0855309。中断调用的云端实际账单仍可能与本地估算略有差异。

### 第二轮离线修复

早停后没有再次调用模型，新增：

1. Generate、Review、Revise 统一执行完整长度区间：普通请求 140–1,600 字符，七天计划
   320–2,400 字符；
2. Review 轨迹同时记录 `minimumAnswerChars` 和 `maximumAnswerChars`，过短或过长都不能
   直接通过；
3. 回放器每写入一条结果就累计 RLVR 违规；一旦超过允许值立即固化
   `stopped_early=true` 和 `stop_reason=rlvr_violations_exceeded`，不再继续产生费用；
4. 恢复已有部分 Manifest 时先检查违规数，已经不可通过的 Batch 不会继续执行。

该部分 Batch 明确为失败证据，不能续跑后包装成通过，也不能替代新的 100/100 Stage 2
验证。完整长度契约只完成了离线测试；下一次真实验证必须使用新 Batch ID，并再次取得明确
模型调用授权。

## v6 最终验收计划

第二轮离线修复已升级为新策略身份 `agentic-rag-v6`，历史 v5 轨迹和报告保持不变。
2026-07-29 已完成最终 Stage 2 的 dry-run 冻结：

| 项目 | 冻结值 |
|---|---|
| Batch | `policy-v6-v2-stage2-final` |
| Policy | `agentic-rag-v6` |
| 计划指纹 | `35504aa5e7bee2b426ecd051088f1c3a7f0c3260fd92f1c7df8577b14a7dbc2d` |
| 种子 / 轮次 / 计划运行 | 50 / 2 / 100 |
| Single / Multi | 50 / 50 |
| 关系 / 育儿 / 家务 / 财务标签运行 | 84 / 40 / 40 / 30 |
| 模型调用 / 付费操作 | 0 / 0 |

该计划随后取得明确授权并真实执行。

## v6 验收早停结果

v6 Batch 在第 16 条出现首个 RLVR 违规后自动停止，剩余 84 条没有执行：

| 指标 | 结果 |
|---|---:|
| 完整轨迹 | 16/100 |
| Single / Multi | 8 / 8 |
| 路由匹配 | 16/16 |
| RLVR 平均分 | 0.739480 |
| RLVR 硬门禁 | 16/16 |
| RLVR 违规 | 1 条 `missing_or_invalid_citation` |
| 调用 / Token / 估算费用 | 106 / 188,968 / ¥0.0614886 |
| 超时 / 恢复 | 0 / 0 |

失败轨迹：

- Seed：`seed-b755f1ca063c7ef6da54`
- Run：`replay-policy-v6-v2-stage2-final-1-381ddb2048a0704f`
- Trajectory：`90d64fa1-b7b9-4e7b-b263-944f439c349a`
- 模式：`ADAPTIVE_MULTI_AGENT`
- 在线 Reward / RLVR：0.3958 / 0.684447

三个 Specialist 都生成了带引用贡献，但 Synthesis 的最终答案没有保留任何 `[来源 n]`。
确定性 Review 正确记录 `citationContractPassed=false`；同时 Review 模型返回了包含引用修正
内容但语法非法的 JSON。旧降级分支在解析异常后直接保留了原候选答案，形成 fail-open。

### 第三轮离线修复

真实回放停止后没有再次调用模型。离线修复规定：

1. Review 异常且候选答案已通过确定性引用和长度契约时，才允许降级保留；
2. Review 异常且任一确定性契约失败时，必须继续执行 Revise；
3. 轨迹只记录一个失败 Review 步骤，并继续记录独立 Revise 证据；
4. 新增非法 Review JSON 不能绕过引用门禁的回归测试；
5. 行为变更升级为新策略身份 `agentic-rag-v7`，不把 v6 失败证据改写为通过。

## v7 验收计划

v7 dry-run 已冻结，未启动后端、未调用模型：

| 项目 | 冻结值 |
|---|---|
| Batch | `policy-v7-v2-stage2-final` |
| Policy | `agentic-rag-v7` |
| 计划指纹 | `5c33862fb433fc9248155bbb64561fa0a3a070a525f6c9006a36dcf093fce3af` |
| 种子 / 轮次 / 计划运行 | 50 / 2 / 100 |
| Single / Multi | 50 / 50 |
| 关系 / 育儿 / 家务 / 财务标签运行 | 84 / 40 / 40 / 30 |
| Dry-run 模型调用 / 付费操作 | 0 / 0 |

该计划随后取得明确授权并真实执行。

## v7 完整回放结果

v7 使用冻结计划真实执行。首次运行完成 93 条后，百炼账户返回
`Arrearage`（账户欠费/状态异常）；SDK 的重试占满阶段级 60 秒，Run
`replay-policy-v7-v2-stage2-final-2-45a66654546de2ed` 被持久化为 `FAILED`。充值后使用
同一 Batch、计划指纹和策略断点恢复：93 条已完成前缀没有重跑，失败 Run 进入第 2 次
attempt，随后完成剩余 7 条。

| 项目 | 结果 |
|---|---:|
| 完整轨迹 | 100/100 |
| Single / Multi | 50 / 50 |
| 关系 / 育儿 / 家务 / 财务标签运行 | 84 / 40 / 40 / 30 |
| 路由匹配 / 偏差 | 100 / 0 |
| 在线 Reward 平均 / 最低 / 最高 | 0.845866 / 0.396400 / 0.950000 |
| RLVR 平均 / 最低 / 最高 | 0.759502 / 0.645776 / 0.859966 |
| RLVR 硬门禁 | 100/100 |
| RLVR 违规 | 0 |
| 成功轨迹调用 / Token / 估算费用 | 634 / 1,105,973 / ¥0.3616176 |
| 恢复 Run / 审计超时 | 1 / 1 |
| 严格 Release Gate | **失败：`timeouts_within_limit`** |

调用、Token 和费用只统计完成轨迹返回的遥测。欠费前失败尝试和两次后端启动时的模型调用
可能产生未返回用量，因此实际账单可能略高。

### 分层结果

| 模式 | 轨迹 | Reward 均值 | RLVR 均值 | RLVR 范围 | 硬门禁 | 调用 | Token | 估算费用 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `SINGLE_AGENT` | 50 | 0.874470 | 0.735597 | 0.645776–0.859966 | 50/50 | 238 | 388,164 | ¥0.1240260 |
| `ADAPTIVE_MULTI_AGENT` | 50 | 0.817262 | 0.783407 | 0.706476–0.858541 | 50/50 | 396 | 717,809 | ¥0.2375916 |

Multi 的 RLVR 均值比 Single 高 0.047810，调用和费用分别约为 Single 的 1.66 倍和
1.92 倍。两种模式承载的任务复杂度不同，所以该差值是分层观察结果，不是随机对照的因果
增益。

| 轮次 | 轨迹 | Single / Multi | Reward 均值 | RLVR 均值 | 硬门禁 | 调用 | Token | 估算费用 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 50 | 25 / 25 | 0.845142 | 0.761928 | 50/50 | 317 | 549,739 | ¥0.1799631 |
| 2 | 50 | 25 / 25 | 0.846590 | 0.757075 | 50/50 | 317 | 556,234 | ¥0.1816545 |

### 决策边界

当前证据允许宣称：

- v7 在 100 条分层轨迹上实现 RLVR 硬门禁 100%、违规 0、路由偏差 0；
- 回放器能从 93 条确定性前缀恢复，且不会重放已经完成的模型调用；
- 一次已归因的外部计费中断被完整保留在恢复与超时审计中。

当前证据不允许宣称：

- v7 Batch 通过了原始零超时 Release Gate；
- Multi 相对 Single 的分数差是因果收益；
- Stage 3、Judge、训练和训练后正式评测已经完成。
