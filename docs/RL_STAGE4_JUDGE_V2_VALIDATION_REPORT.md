# RL Stage 4 Judge v2 真实验证报告

## 结论

Judge v2 已在 40 条具备真实人工标签的轨迹上完成隔离验证，共得到 160/160 个完整四维
输出。预注册规则的正向精度达到 100%，但负向精度只有 16.67%，因此负伪标签被永久关闭。

仅使用 Development 30 条自动校准的“正向或 HOLDOUT”契约，在 Final 10 条上自动放行
6 条，6 条全部为人工正向，正向精度 100%，人工负向误放行为 0，最终门禁通过。

## 隔离与执行完整性

- Judge 契约：`human-light-judge-v2`；
- Assessment Namespace：`policy-v7-stage4-judge-v2-validation`；
- 验证计划指纹：
  `491b3cb5e8bf9e3d13278169b0ece61144810b05776115851028fbb498e77411`；
- 轨迹：40/40；
- Judge 输出：160/160；
- 每个维度：40；
- Judge ID 版本匹配：160/160；
- 不完整面板：0；
- v1 Assessment 覆盖：0；
- 模型调用执行计数：160。

后端启动前通过只读契约端点核对版本、Namespace 和空目录。执行完成后后端已停止。旧目录
`tmp/agent-rl/alignment-assessments` 保持不变。

## 原始维度分布

| 维度 | 平均分 | 平均置信度 |
|---|---:|---:|
| 指令遵循 | 0.8500 | 0.8950 |
| 可执行性 | 0.8300 | 0.9000 |
| 逻辑一致性 | 0.7800 | 0.8275 |
| 严格反审 | 0.3450 | 0.3600 |

严格反审仍对“未展开来源内容”过度敏感。预注册规则因此将 6 条判为负向，但只有 1 条是
人工负向，4 条是人工正向，1 条是人工 HOLDOUT。该维度保留为审计信号，不允许生成负伪
标签，也不再作为正向硬门槛。

## 正向校准契约

校准器只读取 Development 30 条，按 25 字符步长寻找满足以下要求的最小长度门槛：

- 至少 10 个自动正向决策；
- Development 正向精度至少 0.80；
- 自动正向中没有人工负向；
- 指令遵循、可执行性、逻辑一致性均至少 0.60；
- 严格反审只用于审计；
- 不生成负伪标签。

自动得到的最小回答长度为 305 字符。

| 数据切分 | 自动正向 | HOLDOUT | 正向精度 | 负向误放行 |
|---|---:|---:|---:|---:|
| Development 30 | 20 | 10 | 0.800000 | 0 |
| Final 10 | 6 | 4 | 1.000000 | 0 |
| 全部 40 | 26 | 14 | 0.846154 | 0 |

该结果允许继续扩大 Judge v2 正向筛选，但不等于 119 条 RLAIF 数据包已经就绪。下一步只需
评审尚未拥有 v2 输出的 79 条轨迹，已有 40 条必须复用；扩量后再按同一冻结契约检查样本
数量与 Stage 5 Readiness。

## 费用说明

执行器记录了 160 个成功 Judge 调用。应用尚未持久化 Judge Token 遥测，因此不能把规划
上界当作精确账单；本轮规划费用上界约 ¥0.1130556。

