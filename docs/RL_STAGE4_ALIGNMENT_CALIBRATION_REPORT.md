# RL Stage 4 人工锚点与聚合校准报告

## 结论

30 条人工锚点已完整导入并通过内容指纹校验，但第一版四维聚合校准没有通过冻结退出门禁，
因此不得导出训练数据或启动百炼训练。该结果被保留为失败证据，不通过事后移动阈值掩盖。

本轮全程离线，模型调用为 0。

## 人工标签

- 完成：30/30；
- 总体平均分：4.033333；
- 正向锚点（4～5 分）：23；
- 保留锚点（3 分）：6；
- 负向锚点（1～2 分）：1；
- 标签指纹：
  `cc44fa0cd226c3bb1eaa4ef082ee0dc09855c2d7e9f67db0c197a71bc7c21056`。

校验器逐条绑定 Batch、计划、轨迹、问题和回答指纹，并拒绝缺项、乱序或内容变更。

## 校准契约

`dimension-aware-alignment-calibration-v1` 固定使用前 20 条拟合、后 10 条留出验证。每个
Judge 维度分别对“分数 + 置信度”做 Ridge 校准，再以等权维度奖励组成 AI Reward；
Verifier/AI 权重固定为 0.45/0.55，正向阈值固定为 0.75，最低置信度固定为 0.72。

四维拟合 MAE：

| 维度 | MAE |
|---|---:|
| 指令遵循 | 0.148738 |
| 可执行性 | 0.222924 |
| 逻辑一致性 | 0.114172 |
| 严格反审 | 0.160977 |

严格反审维度的 Judge 系数为负，说明它在这批锚点上不是可靠的正向质量信号，不能继续沿用
旧的“不同维度必须彼此一致”聚合假设。

## 冻结门禁结果

20 条拟合集正向精度为 0.777778；原 10 条留出集正向精度为 0.666667，并误将 1 条人工
负向锚点判为正向。两项门禁失败：

1. 留出集正向精度至少 0.80；
2. 不得自动放行人工负向锚点。

虽然校准后 119 条中会产生 107 条正向、11 条 HOLDOUT、1 条 EXCLUDED，伪标签覆盖率为
0.906780，但失败门禁优先于覆盖率，107 条不得作为训练正样本导出。校准报告指纹：
`075ac7c1ad21c4ac237cd6a9d8f9a76923535cb06d0fdd7cf2c314d0b31719c2`。

## 新鲜最终盲测

原 10 条留出集已用于诊断，不能再充当最终无偏证据。为避免重复标注 20 条拟合集或重新
调用 Judge，已从其余 89 条从未人工查看的轨迹冻结 10 条新鲜挑战样本：

- actions、checklist、decision、dialogue、weekly_plan 各 2 条；
- Single Agent 与 Adaptive Multi Agent 各 5 条；
- 每个“请求类型 × 执行模式”组合恰好 1 条；
- 页面不包含 Judge 分数、置信度或理由；
- 不包含种子参考答案；
- 与前 30 条轨迹无重叠；
- 模型调用 0。

盲测包指纹：
`6bf4b2feb4c4e31769ee46b41e9f578a2b9634a78b2d24e3281f439147d1939e`。
只有这 10 条完成并通过固定门禁后，才允许确定最终聚合契约和进入训练数据导出。

